spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://citric-blunt-exemption.ngrok-free.dev/auth/realms/default
          jwk-set-uri: https://citric-blunt-exemption.ngrok-free.dev/auth/realms/default/protocol/openid-connect/certs

logging:
  level:
    org:
      springframework:
        security: DEBUG

jwt:
  auth:
    converter:
      resource-id: demo
      principle-attribute: preferred_username
backend:
  base-url: http://localhost:8080